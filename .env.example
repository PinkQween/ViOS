# Remote build configuration for buildExternal.sh
# Copy this file to .env and replace these values with your actual remote server details

# Remote server connection details
USER=your_username
HOST=your_server_ip_or_hostname
REMOTE_PATH=/path/to/remote/project/directory

# Authentication (choose one method)
# Option 1: Password authentication
# PASSWORD=your_password

# Option 2: SSH key authentication (uncomment and set if using SSH keys)
# IDENTITY_FILE=/path/to/your/private/key

# Example configurations:
# USER=john
# HOST=192.168.1.100
# REMOTE_PATH=/home/john/vios-build
# PASSWORD=mypassword123

# Or for SSH key authentication:
# USER=john
# HOST=192.168.1.100
# REMOTE_PATH=/home/john/vios-build
# IDENTITY_FILE=~/.ssh/id_rsa 